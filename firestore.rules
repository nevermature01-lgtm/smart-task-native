rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Sab collections ke liye
    match /{document=**} {
      allow read, write: if request.auth != null;
    }

  }
}